# Timeline Layout

Use `timeline` for chronological milestones.

Recommended spec:

- `events`: 3-8 events.
- Each event should include `date`, `title`, and optional `text`.
- Keep event text short.

Rendering behavior:

- Timeline axis is horizontal.
- Event cards alternate above and below the axis.
- Dates are highlighted in coral.
